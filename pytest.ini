[pytest]
testpaths = tests
# Pure-logic tests run with no env. DB integration tests need a throwaway DB:
#   MERCURY_TEST_DB=hermes_test python -m pytest tests/ -q
# (hermes_test must have the pgvector extension; created once via the postgres superuser.)
filterwarnings =
    ignore::DeprecationWarning
